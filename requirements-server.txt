# Deps do SERVIDOR (VPS/Easypanel). Não inclui Playwright/pdfplumber/Streamlit
# — o servidor não raspa nem renderiza; só fila + classificação + Sheets + painel.
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
google-genai>=0.3
gspread>=6.0
google-auth>=2.30
pyyaml>=6.0
python-dotenv>=1.0
